# Web Summarizer

Aplicação web para resumir textos usando Inteligência Artificial.

## Tecnologias utilizadas

- FastAPI (Python)
- React (Vite)
- Git

## Processo para rodar o projeto

### Backend

```bash
cd backend
venv\Scripts\activate
pip install -r requirements.txt
uvicorn main:app --reload
```